apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: om-pv-claim
spec:
  storageClassName: standard
  volumeMode: Filesystem
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 3Gi
