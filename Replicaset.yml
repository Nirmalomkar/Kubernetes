apiVersion: apps/v1
kind:  ReplicaSet
metadata: 
  name: Omiireplica-set
  labels:
    app: Omii
spec:
    selector: 
      matchExpressions:      
         #-  { key: app, operator: In , values: [rsapp,  new-app]}
         -  { key: app, operator: In , values: [Omiireplica-set, new-app]}
    #matchLabels: 
          #app: rsapp
    replicas: 5
    template:
      metadata:
        labels:
          name: nginxapp
          app: Omii
      spec:
        containers:
          - name: nginxapp
            image: nginx:latest 
            ports:
                - containerPort: 80
                  protocol: TCP
